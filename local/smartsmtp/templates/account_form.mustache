{{!
  @template local_smartsmtp/account_form
  Formulario de alta/edición de buzón SMTP.
  Renderizado por edit_account.php via $form->display() de moodleform.
  Este template no se usa directamente — moodleform genera su propio HTML.
  Se deja como placeholder requerido por el directorio de templates.
}}
{{! moodleform renderiza el formulario directamente con display() }}
